# Starting point: basic Python, new streaming concepts

The learner reports basic Python knowledge but is new to async programming, SSE, streaming proxies, and the Responses API. Future lessons should use Python syntax as a base while explicitly teaching the streaming concepts through CodexCont rather than assuming prior familiarity.

## Evidence

The learner explicitly confirmed this target-reader profile during the learning-file design session.

## Implications

Start with the smallest async/SSE mental model needed to trace `incremental_sse()` and `fold_stream()` before teaching broader maintenance work.
