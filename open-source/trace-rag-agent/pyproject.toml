[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trace-rag-agent"
version = "0.1.0"
description = "An observable, human-gated RAG Agent prototype for field delivery workflows."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["rag_agent*"]
