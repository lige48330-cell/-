# Domain docs

This repository uses a single-context documentation layout.

Before work that depends on domain terminology or architectural decisions, read root `CONTEXT.md` and relevant files in `docs/adr/` when they exist. If neither exists, proceed without inventing a glossary; create those documents only when a real terminology or architecture decision needs to be recorded.
